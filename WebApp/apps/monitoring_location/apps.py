from django.apps import AppConfig

class MonitoringLocationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.monitoring_location'
    verbose_name = 'Location Monitoring'
